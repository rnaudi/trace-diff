# trace-diff

Structural diff for distributed traces.

Datadog doesn't have trace diffing.

## Usage

`deno run --allow-all make.ts` builds a self-contained HTML file.

Open it, drop two trace files.
